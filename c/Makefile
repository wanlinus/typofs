# 本版仅用 Windows SDK（WinHTTP + CNG），无需 vcpkg / curl / openssl。
# 推荐用 CMake + MSVC（见 CMakeLists.txt）或一键脚本 build.bat。
# GNU make 不适用 MSVC；若要直接用 cl：
#   cl /utf-8 /std:c11 /O2 main.c /Fe:typofs.exe winhttp.lib bcrypt.lib
# 产物：typofs.exe
